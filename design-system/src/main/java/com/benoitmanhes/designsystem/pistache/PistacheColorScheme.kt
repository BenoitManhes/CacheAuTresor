package com.benoitmanhes.designsystem.pistache

interface PistacheColorScheme {
    // Content (Text & Icon)

    // Stroke (Border & Divider)

    // Surface

    // Background

    // Gradient

    // Ripple (not required if RippleTheme works)

    /** Variant
     * - Primary, Secondary, Tertiary, …
     * - Disable
     * - Critical
     * - Soft/Strong (alpha)
     * - Level (for surface)
     * - Dark/Light (luminance)
     */
}
